import { FormationRequestModel } from './formation-request.model';

describe('FormationRequest', () => {
  it('should create an instance', () => {
    expect(new FormationRequestModel()).toBeTruthy();
  });
});
