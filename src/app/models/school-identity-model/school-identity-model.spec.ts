import { SchoolIdentityModel } from './school-identity-model';

describe('SchoolIdentityModel', () => {
  it('should create an instance', () => {
    expect(new SchoolIdentityModel()).toBeTruthy();
  });
});
