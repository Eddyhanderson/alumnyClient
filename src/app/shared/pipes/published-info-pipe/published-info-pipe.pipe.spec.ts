import { PublishedInfoPipePipe } from './published-info-pipe.pipe';

describe('PublishedInfoPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PublishedInfoPipePipe();
    expect(pipe).toBeTruthy();
  });
});
