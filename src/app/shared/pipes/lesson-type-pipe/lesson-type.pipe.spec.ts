import { LessonTypePipe } from './lesson-type.pipe';

describe('LessonTypePipe', () => {
  it('create an instance', () => {
    const pipe = new LessonTypePipe();
    expect(pipe).toBeTruthy();
  });
});
