import { DataFormaterDirective } from './data-formater.directive';

describe('DataFormaterDirective', () => {
  it('should create an instance', () => {
    const directive = new DataFormaterDirective();
    expect(directive).toBeTruthy();
  });
});
