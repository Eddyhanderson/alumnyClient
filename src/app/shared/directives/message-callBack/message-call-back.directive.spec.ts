import { MessageCallBackDirective } from './message-call-back.directive';

describe('MessageFallBackDirective', () => {
  it('should create an instance', () => {
    const directive = new MessageCallBackDirective(null);
    expect(directive).toBeTruthy();
  });
});
